{
	"name": "bawk-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build:client": "esbuild client/src/game.ts --bundle --format=esm --sourcemap --outfile=public/game.js",
		"deploy": "wrangler deploy",
		"dev": "npm run build:client && wrangler dev",
		"start": "npm run dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"three": "^0.184.0"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.8.0",
		"esbuild": "^0.28.0",
		"playwright-core": "^1.60.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.92.0"
	}
}
